# Solace Chat Application Login Server
A server that handles login requests and proxies the request through a REST-ful POST to the PubSub+ Software Broker.

To run the web-server, type the following command:
```
mvn spring-boot:run
```